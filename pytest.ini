[pytest]
addopts = -q --maxfail=1 --disable-warnings --cov=aeonforge --cov-report=term-missing --cov-fail-under=90
testpaths = tests
pythonpath = src
markers =
    perf: performance measurements (non-failing)
